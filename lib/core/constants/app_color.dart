import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Theme
  static const Color primaryColor = Color(0xff5D5CFF);
  static const Color backgroundColor = Colors.white;
  static const Color errorColor = Colors.redAccent;

  // Text
  static const Color textColor = Colors.black;
  static const Color secondaryTextColor = Color(0xff6F6F6F);
  static const Color buttonTextColor = Colors.white;

  // Textfield
  static const Color textFieldHintColor = Color(0xff828290);
  static const Color borderColor = Color(0xffD7D7FF);
}
